node_modules/
.vite/
dist/
release/
.DS_Store
*.log
.env
.env.local
*.tsbuildinfo
.npmrc
CLAUDE.md
GEMINI.md

# Agent 协作约定与架构决策记录（仅本地维护，永不进仓库）
AGENTS.md
docs/agents/
docs/adr/
/.claude
/.codex
/.gemini
/.cc-switch
/.idea
/.vscode
vitest-report.json
nul

# 临时文件和备份文件
*.backup
temp_*
temp/
*.bak
*.old
*.tmp
*.temp
.sync_logs_tmp/
errors*.txt
tsc*.txt
typecheck*.txt

# 编译输出
out/

# app.asar 手动解包副本（打包调试/冒烟测试用，可重新解包生成）
.asar-temp/

# 测试覆盖率报告
coverage/

# 测试和调试文件
test_*.js
test_*.ts

# Flatpak build artifacts
flatpak/cc-switch.deb
flatpak-build/
flatpak-repo/
.worktrees/
.spec-workflow/
copilot-api
.history
CODEBUDDY.md
.codegraph
.superpowers
docs/superpowers
docs/framework
# 探针调试文件
tmp/

# Google OAuth 本地密钥配置（含真实 client secret，禁止提交）
config/google-oauth.local.json

# 自定义域名部署副本（本地构建用，不提交）
website-h/

# 本地任务跟踪（根因分析等，不提交）——仅限项目根目录，避免误伤 tests/**/tasks/
/tasks/

docs/superpowers/
docs/architecture/
