/**
 * MCP SSE Transport — 基于官方 SDK SSEClientTransport
 *
 * @deprecated SSE 传输已被 MCP 规范废弃，推荐使用 StreamableHTTP。
 * 仅用于兼容旧 MCP Server。
 */

export { SSEClientTransport } from '@modelcontextprotocol/sdk/client/sse.js'
