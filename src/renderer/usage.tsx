import React from 'react'
import ReactDOM from 'react-dom/client'
import UsageWindow from './components/usage/UsageWindow'
import './styles/globals.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
    <React.StrictMode>
        <UsageWindow/>
    </React.StrictMode>,
)
